<UserControl x:Class="WPFApplication.Issues.Views.AddToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" >
    <ToolBar Background="Transparent">
        <ToggleButton IsChecked="{Binding IsMapVisible}" Command="{Binding PositionCommand}">
            <TextBlock>Positionera</TextBlock>
        </ToggleButton>
        <Separator />
        <Button Command="{Binding AddCommand}">
            <TextBlock>Lägg till</TextBlock>
        </Button>
        <Button Command="{Binding CancelCommand}">
            <TextBlock>Avbryt</TextBlock>
        </Button>
    </ToolBar>
</UserControl>
